# Validate an mcgf_rs object

Validate an mcgf_rs object

## Usage

``` r
validate_mcgf_rs(x)
```

## Arguments

- x:

  An mcgf_rs object

## Value

An S3 object of class `mcgf_rs`.

## Details

It validates an `mcgf_rs` object by checking if `label` is of the
matching length.
